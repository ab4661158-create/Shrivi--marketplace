# ARCHIVED: the authoritative Android pipeline is build-android-final.yml.
# This legacy workflow is manual-only to prevent duplicate/conflicting APK builds.
name: Build SHRIVI Android APK (Legacy - Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Explain archived pipeline
        run: echo 'Legacy Android workflow disabled. Use build-android-final.yml.'
