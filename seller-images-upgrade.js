const { Pool } = require('pg');
const multer = require('multer');
const { v2: cloudinary } = require('cloudinary');
const express = require('express');

const pool = new Pool({connectionString:process.env.DATABASE_URL,ssl:process.env.DATABASE_URL?{rejectUnauthorized:false}:false,max:5,idleTimeoutMillis:30000,connectionTimeoutMillis:10000});
cloudinary.config({cloud_name:process.env.CLOUDINARY_CLOUD_NAME,api_key:process.env.CLOUDINARY_API_KEY,api_secret:process.env.CLOUDINARY_API_SECRET});
const upload=multer({storage:multer.memoryStorage(),limits:{files:8,fileSize:5*1024*1024},fileFilter:(req,file,cb)=>{const a=['image/jpeg','image/png','image/webp','image/gif'];if(!a.includes(file.mimetype))return cb(new Error('Only JPG, PNG, WEBP and GIF images are allowed.'));cb(null,true)}});
const clean=v=>String(v??'').trim();
function seller(req,res,next){if(!req.session?.seller)return res.status(401).json({error:'Seller login required'});next()}
const num=v=>Number.isFinite(Number(v))?Number(v):0;
function salePrice(p,d){return Math.round((num(p)-num(p)*num(d)/100)*100)/100}
function uploadOne(file){return new Promise((resolve,reject)=>{if(!process.env.CLOUDINARY_CLOUD_NAME||!process.env.CLOUDINARY_API_KEY||!process.env.CLOUDINARY_API_SECRET)return reject(new Error('Image service is not configured.'));const s=cloudinary.uploader.upload_stream({folder:'shrivi/products'},(e,r)=>e?reject(e):resolve(r.secure_url||r.url));s.end(file.buffer)})}
async function ensureTables(){await pool.query(`CREATE TABLE IF NOT EXISTS product_images(id BIGSERIAL PRIMARY KEY,product_id BIGINT NOT NULL REFERENCES products(id) ON DELETE CASCADE,image_url TEXT NOT NULL,sort_order INTEGER NOT NULL DEFAULT 0,is_primary BOOLEAN NOT NULL DEFAULT FALSE,created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());CREATE INDEX IF NOT EXISTS idx_product_images_product ON product_images(product_id,sort_order);`)}
async function gallery(id,fallback){const r=await pool.query('SELECT image_url FROM product_images WHERE product_id=$1 ORDER BY is_primary DESC,sort_order ASC,id ASC',[id]);const u=r.rows.map(x=>x.image_url).filter(Boolean);return u.length?u:(fallback?[fallback]:[])}
function payload(b){const name=clean(b.name),category=clean(b.category),description=clean(b.description),price=num(b.price),stock=Number(b.stock),discount=num(b.discount_percent);if(!name)throw Error('Product name is required');if(price<0||price>100000000)throw Error('Valid product price is required');if(!Number.isInteger(stock)||stock<0)throw Error('Stock must be a whole number 0 or greater');if(discount<0||discount>99.99)throw Error('Discount must be between 0 and 99.99');return{name,category:category||null,description:description||null,price,stock,discount_percent:discount}}
async function install(app){if(app.__shriviSellerImages)return;app.__shriviSellerImages=true;await ensureTables();
// Compatibility endpoint: the current 8-slot UI uploads one file at a time.
app.post('/api/seller/upload/image',seller,(req,res)=>{upload.single('image')(req,res,async e=>{try{if(e)return res.status(400).json({error:e.message});if(!req.file)return res.status(400).json({error:'Image file is required'});const url=await uploadOne(req.file);res.json({ok:true,image:url,url,secure_url:url})}catch(x){console.error('Seller single image upload:',x);res.status(500).json({error:x.message||'Image upload failed'})}})});
// Batch endpoint remains supported for clients that send all selected images together.
app.post('/api/seller/upload/images',seller,(req,res)=>{upload.array('images',8)(req,res,async e=>{try{if(e)return res.status(400).json({error:e.message});const files=req.files||[];if(!files.length)return res.status(400).json({error:'Select at least one product image.'});const urls=await Promise.all(files.map(uploadOne));res.json({ok:true,images:urls})}catch(x){res.status(500).json({error:x.message||'Image upload failed'})}})});
app.get('/api/seller/products',seller,async(req,res)=>{try{const r=await pool.query('SELECT p.*,s.name AS seller_name FROM products p LEFT JOIN sellers s ON s.id=p.seller_id WHERE p.seller_id=$1 ORDER BY p.id DESC',[Number(req.session.seller.id)]);const out=[];for(const p of r.rows){const images=await gallery(p.id,p.image);out.push({...p,price:num(p.price),original_price:num(p.price),discount_percent:num(p.discount_percent),sale_price:salePrice(p.price,p.discount_percent),stock:Math.max(0,Number(p.stock)||0),images,image_gallery:images})}res.json(out)}catch(e){res.status(500).json({error:e.message||'Unable to load products'})}});
app.post('/api/seller/products',seller,async(req,res)=>{const c=await pool.connect();try{const p=payload(req.body||{});let images=Array.isArray(req.body?.images)?req.body.images.map(clean).filter(Boolean).slice(0,8):[];if(!images.length&&clean(req.body?.image))images=[clean(req.body.image)];if(!images.length)return res.status(400).json({error:'Add at least one product image.'});await c.query('BEGIN');const r=await c.query('INSERT INTO products(name,price,category,image,description,stock,discount_percent,seller_id) VALUES($1,$2,$3,$4,$5,$6,$7,$8) RETURNING *',[p.name,p.price,p.category,images[0],p.description,p.stock,p.discount_percent,Number(req.session.seller.id)]);for(let i=0;i<images.length;i++)await c.query('INSERT INTO product_images(product_id,image_url,sort_order,is_primary) VALUES($1,$2,$3,$4)',[r.rows[0].id,images[i],i,i===0]);await c.query('COMMIT');res.status(201).json({ok:true,product:{...r.rows[0],images,image_gallery:images,sale_price:salePrice(p.price,p.discount_percent)}})}catch(e){await c.query('ROLLBACK');res.status(400).json({error:e.message||'Unable to create product'})}finally{c.release()}});
app.put('/api/seller/products/:id',seller,async(req,res)=>{const c=await pool.connect();try{const id=Number(req.params.id),own=await c.query('SELECT id,image FROM products WHERE id=$1 AND seller_id=$2',[id,Number(req.session.seller.id)]);if(!own.rows.length)return res.status(404).json({error:'Product not found'});const p=payload(req.body||{});let images=Array.isArray(req.body?.images)?req.body.images.map(clean).filter(Boolean).slice(0,8):[];if(!images.length&&clean(req.body?.image))images=[clean(req.body.image)];if(!images.length)images=await gallery(id,own.rows[0].image);if(!images.length)return res.status(400).json({error:'Add at least one product image.'});await c.query('BEGIN');const r=await c.query('UPDATE products SET name=$1,price=$2,category=$3,image=$4,description=$5,stock=$6,discount_percent=$7 WHERE id=$8 AND seller_id=$9 RETURNING *',[p.name,p.price,p.category,images[0],p.description,p.stock,p.discount_percent,id,Number(req.session.seller.id)]);await c.query('DELETE FROM product_images WHERE product_id=$1',[id]);for(let i=0;i<images.length;i++)await c.query('INSERT INTO product_images(product_id,image_url,sort_order,is_primary) VALUES($1,$2,$3,$4)',[id,images[i],i,i===0]);await c.query('COMMIT');res.json({ok:true,product:{...r.rows[0],images,image_gallery:images,sale_price:salePrice(p.price,p.discount_percent)}})}catch(e){await c.query('ROLLBACK');res.status(400).json({error:e.message||'Unable to update product'})}finally{c.release()}});
app.get('/api/seller/products/:id/images',seller,async(req,res)=>{try{const id=Number(req.params.id),r=await pool.query('SELECT id,image FROM products WHERE id=$1 AND seller_id=$2',[id,Number(req.session.seller.id)]);if(!r.rows.length)return res.status(404).json({error:'Product not found'});res.json({ok:true,images:await gallery(id,r.rows[0].image)})}catch(e){res.status(500).json({error:e.message||'Unable to load images'})}});
}
const listen=express.application.listen;express.application.listen=function(){const app=this;install(app).catch(e=>console.error('SHRIVI SELLER IMAGE UPGRADE:',e));return listen.apply(this,arguments)};
